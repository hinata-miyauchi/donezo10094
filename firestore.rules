rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /issues/{issueId} {
      allow read, write: if true;  // 開発環境用の設定
    }
    match /lastIssueNumber/{document=**} {
      allow read, write: if true;  // 開発環境用の設定
    }
  }
}